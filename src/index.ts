type AdressObj = {
    addressNo: number;
    roadAddress: string;
    city: string;
    state: string;
    country: string;
};

const addressToObject = (address: string): AdressObj => {
    return {};
};

const makeShortAddress = ({ addressNo, roadAddress, city, state, country }: AdressObj) => {};
